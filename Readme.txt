Prerequirements:

Install PSTools, and specify path in config.ini file.

Specify all labels in the config file.

Features:
Add clients using the clients.txt, or by using the Add New Computer in LabRun(recommended by non IT folks).
Monitor Clients
Better feedback and error handling.
InputBlocker works and ScreenShare works.

Stuff to do:
Fix better config errors / feedback.
Make a static config file with all the required inputs
# Make a method that can delete temp files on the local Computer and Cookies
Bugs:
# Crash when trying to sharescreen multiple times / Might be fixed with catch exception in Service.RunCMD

# FIX ALL INTEGEGRATION WITH M:Drive 

# FIX MERGE
